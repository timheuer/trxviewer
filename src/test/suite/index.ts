// This file is used as the entry point for VS Code's test runner
// Jest tests will be run separately via the jest CLI

export async function run(): Promise<void> {
  // We're using Jest directly from runTest.ts now
  return Promise.resolve();
}
